# Copy to .env and fill in. The .env file is gitignored.
# Get this from a logged-in browser:
#   DevTools > Application > Cookies > https://www.strava.com
#   copy the VALUE of the `_strava4_session` cookie.
# It expires periodically; re-paste when fetches start returning 401.
_STRAVA4_SESSION=
